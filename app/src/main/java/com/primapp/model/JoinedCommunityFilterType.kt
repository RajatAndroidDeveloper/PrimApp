package com.primapp.model

object JoinedCommunityFilterType {
    const val ALL = "all"
    const val INACTIVE = "inactive"

    //By default if we don't even send type
    const val ACTIVE = "active"
}